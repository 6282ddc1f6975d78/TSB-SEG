name: bottomup
task: change_point

instance:
  _target_: tsseg.algorithms.bottomup.detector.BottomUpDetector

  n_cps: null
  model: "l2"
  min_size: 2
  jump: 5
  penalty: 10       # Switches to None if n_cps is not None
  epsilon: null
  cost_params: null

tunable_parameters:
  - model: ["l1", "l2", "rbf"]
  - penalty: [10, 100]